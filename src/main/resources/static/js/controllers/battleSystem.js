

export function battleSystem(k, ctx, monsterData) {

  let playerStatus = {
    playerHp: ctx.atributos.hp,
    playerAtk: ctx.atributos.forca,
    playerDef: ctx.atributos.defesa,
  }
  let monterStatus = {
    monsterData: monsterData.hp,
    mo
  }

}
